import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
  appId: "br.com.cpusis.pontonorte",
  appName: "PontoNorte",
  webDir: "dist",
  android: { allowMixedContent: false },
};
export default config;
